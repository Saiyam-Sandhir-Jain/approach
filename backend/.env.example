# ── Database ──────────────────────────────────────────────────────────────────
# Local SQLite (default, no setup needed):
DATABASE_URL=sqlite:///./crm.db

# Production PostgreSQL (Neon / Supabase / any Postgres):
# DATABASE_URL=postgresql://user:password@host:5432/approach
